// Instant navigation fallback for / — shown while the route segment loads.
import { HomePageSkeleton } from "@/components/Skeleton";

export default function Loading() {
  return <HomePageSkeleton />;
}
