# KG change pipeline — handoff

**Current phase:** none started. Plan approved 2026-08-17 (`KG_CHANGE_PIPELINE_PLAN.md`).

**Worktree:** `C:\Users\devag\Documents\Code (local)\order-workflows-kg`, branch
`feat/kg-change-pipeline` (from `demo/dialogue-plus-run` @ `0a6e84d`). Needs its own `.venv`
(`pip install -e ".[dev]"`), a copied `.env`, and `npm install` in `frontend/` before use.

**Inputs already researched:** see `research/*.md`. The manager's repo is NOT yet copied into
`examples/` — Phase 0 does that (clone `github.com/SoumyajitPodder/Intelligent_Workflow_Builder`
branch `Sample_Doc_Code`; copy `Existing_KG/` → `examples/knowledge_bases/order-lifecycle/`,
`New_business-change/BCR-001-partial-shipment-support.docx` → `examples/change_requests/`).
KG-Context source to vendor: `C:\Users\devag\Documents\Code (local)\KG-Context-bm-devansh` @ `0447cad`.

**Next action:** start Phase 0 (§2 of the plan). Read §0 + Phase 0 + `research/kg-context-digest.md`.

## Phase log
- (empty)
